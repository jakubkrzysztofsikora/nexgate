repos:
  - repo: local
    hooks:
      - id: gitleaks
        name: gitleaks staged secret scan
        entry: gitleaks protect --staged --redact --verbose
        language: system
        pass_filenames: false
